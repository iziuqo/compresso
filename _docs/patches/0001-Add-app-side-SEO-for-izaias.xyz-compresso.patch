From 8107774801bda8c5e2552914316a240a4638fc5d Mon Sep 17 00:00:00 2001
From: Cursor Agent <cursoragent@cursor.com>
Date: Wed, 19 Aug 2026 19:40:21 +0000
Subject: [PATCH] Add app-side SEO for izaias.xyz/compresso

- Consumer-focused meta, OG, and WebApplication JSON-LD in all 7 locales
- Post-build locale HTML shells with hreflang, sitemap.xml, robots.txt
- Path-based locale URLs (/compresso/es/, etc.) synced on language switch
- Footer links to docs, FAQ, and npm on compresso.izaias.xyz
---
 index.html                    |  16 ++-
 package.json                  |   1 +
 public/robots.txt             |   4 +
 scripts/generate-seo.mjs      | 216 ++++++++++++++++++++++++++++++++++
 src/i18n/index.tsx            |   6 +
 src/i18n/locales/de.json      |   7 ++
 src/i18n/locales/en.json      |   7 ++
 src/i18n/locales/es.json      |   7 ++
 src/i18n/locales/fr.json      |   7 ++
 src/i18n/locales/it.json      |   7 ++
 src/i18n/locales/pt-BR.json   |   7 ++
 src/i18n/locales/zh-Hans.json |   7 ++
 src/seo/constants.ts          |  50 ++++++++
 src/seo/document-meta.ts      | 124 +++++++++++++++++++
 src/seo/footer-links.ts       |  12 ++
 src/seo/locale-path.ts        |  30 +++++
 src/styles/app.css            |   7 ++
 src/ui/Chrome.tsx             |  12 +-
 vercel.json                   |  12 ++
 19 files changed, 536 insertions(+), 3 deletions(-)
 create mode 100644 public/robots.txt
 create mode 100644 scripts/generate-seo.mjs
 create mode 100644 src/seo/constants.ts
 create mode 100644 src/seo/document-meta.ts
 create mode 100644 src/seo/footer-links.ts
 create mode 100644 src/seo/locale-path.ts

diff --git a/index.html b/index.html
index 573f387..7e2de99 100644
--- a/index.html
+++ b/index.html
@@ -5,12 +5,24 @@
     <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
     <meta name="theme-color" content="#FFFFFF" media="(prefers-color-scheme: light)" />
     <meta name="theme-color" content="#000000" media="(prefers-color-scheme: dark)" />
-    <meta name="description" content="Compress, resize and convert images. Runs entirely on your device — nothing is uploaded. Works offline." />
+    <meta name="description" content="Free private image compressor. Resize and convert HEIC, JPEG, PNG, WebP, and AVIF on your device. Nothing is uploaded. Works offline as a PWA." />
+    <meta name="robots" content="index, follow" />
     <meta name="apple-mobile-web-app-capable" content="yes" />
     <meta name="apple-mobile-web-app-status-bar-style" content="default" />
     <link rel="icon" href="%BASE_URL%icons/favicon.svg" type="image/svg+xml" />
     <link rel="apple-touch-icon" href="%BASE_URL%icons/apple-touch-icon.png" />
-    <title>Compresso</title>
+    <link rel="canonical" href="https://izaias.xyz/compresso/" />
+    <meta property="og:title" content="Compresso — private image compression in your browser" />
+    <meta property="og:description" content="Compress photos on your device. 0 bytes uploaded. HEIC support, batch processing, works offline." />
+    <meta property="og:url" content="https://izaias.xyz/compresso/" />
+    <meta property="og:site_name" content="Compresso" />
+    <meta property="og:type" content="website" />
+    <meta property="og:image" content="https://compresso.izaias.xyz/og-image.jpg" />
+    <meta name="twitter:card" content="summary_large_image" />
+    <meta name="twitter:title" content="Compresso — private image compression in your browser" />
+    <meta name="twitter:description" content="Compress photos on your device. 0 bytes uploaded. HEIC support, batch processing, works offline." />
+    <meta name="twitter:image" content="https://compresso.izaias.xyz/og-image.jpg" />
+    <title>Compresso — Compress Images Online Free | Private, No Upload</title>
   </head>
   <body>
     <div id="root"></div>
diff --git a/package.json b/package.json
index 5895632..8c3fd8a 100644
--- a/package.json
+++ b/package.json
@@ -6,6 +6,7 @@
   "scripts": {
     "dev": "vite",
     "build": "tsc --noEmit && vite build",
+    "postbuild": "node scripts/generate-seo.mjs",
     "preview": "vite preview",
     "icons": "node scripts/icons.mjs"
   },
diff --git a/public/robots.txt b/public/robots.txt
new file mode 100644
index 0000000..0aa67b6
--- /dev/null
+++ b/public/robots.txt
@@ -0,0 +1,4 @@
+User-agent: *
+Allow: /compresso/
+
+Sitemap: https://izaias.xyz/compresso/sitemap.xml
diff --git a/scripts/generate-seo.mjs b/scripts/generate-seo.mjs
new file mode 100644
index 0000000..d9bbf78
--- /dev/null
+++ b/scripts/generate-seo.mjs
@@ -0,0 +1,216 @@
+#!/usr/bin/env node
+/**
+ * Post-build: locale-specific index.html shells, sitemap.xml, robots.txt.
+ * Crawlers get translated meta + hreflang in static HTML; the SPA hydrates the same bundle.
+ */
+import { readFileSync, writeFileSync, mkdirSync, existsSync } from 'node:fs';
+import { dirname, join } from 'node:path';
+import { fileURLToPath } from 'node:url';
+
+const __dirname = dirname(fileURLToPath(import.meta.url));
+const ROOT = join(__dirname, '..');
+const OUT = join(ROOT, 'dist/compresso');
+
+const APP_ORIGIN = 'https://izaias.xyz';
+const APP_BASE = '/compresso';
+const SITE_URL = 'https://compresso.izaias.xyz';
+const OG_IMAGE = `${SITE_URL}/og-image.jpg`;
+const BUILD_DATE = new Date().toISOString().slice(0, 10);
+
+const LOCALE_FILES = {
+  en: 'en.json',
+  es: 'es.json',
+  fr: 'fr.json',
+  de: 'de.json',
+  it: 'it.json',
+  'pt-BR': 'pt-BR.json',
+  'zh-Hans': 'zh-Hans.json',
+};
+
+const LOCALE_SLUGS = {
+  en: 'en',
+  es: 'es',
+  fr: 'fr',
+  de: 'de',
+  it: 'it',
+  'pt-BR': 'pt-br',
+  'zh-Hans': 'zh-hans',
+};
+
+const HREFLANG = {
+  en: 'en',
+  es: 'es',
+  fr: 'fr',
+  de: 'de',
+  it: 'it',
+  'pt-BR': 'pt-BR',
+  'zh-Hans': 'zh-Hans',
+};
+
+const HTML_LANG = {
+  en: 'en',
+  es: 'es',
+  fr: 'fr',
+  de: 'de',
+  it: 'it',
+  'pt-BR': 'pt-BR',
+  'zh-Hans': 'zh-Hans',
+};
+
+function loadMeta(locale) {
+  const raw = readFileSync(join(ROOT, 'src/i18n/locales', LOCALE_FILES[locale]), 'utf8');
+  const dict = JSON.parse(raw);
+  const en = JSON.parse(readFileSync(join(ROOT, 'src/i18n/locales/en.json'), 'utf8'));
+  return {
+    title: dict['meta.title'] ?? en['meta.title'],
+    description: dict['meta.description'] ?? en['meta.description'],
+    ogTitle: dict['meta.ogTitle'] ?? en['meta.ogTitle'],
+    ogDescription: dict['meta.ogDescription'] ?? en['meta.ogDescription'],
+  };
+}
+
+function appUrl(locale) {
+  if (locale === 'en') return `${APP_ORIGIN}${APP_BASE}/`;
+  return `${APP_ORIGIN}${APP_BASE}/${LOCALE_SLUGS[locale]}/`;
+}
+
+
+function webAppSchema(locale, meta) {
+  return {
+    '@context': 'https://schema.org',
+    '@type': 'WebApplication',
+    name: 'Compresso',
+    url: appUrl(locale),
+    applicationCategory: 'MultimediaApplication',
+    operatingSystem: 'Any (web browser)',
+    description: meta.description,
+    inLanguage: HREFLANG[locale],
+    offers: { '@type': 'Offer', price: '0', priceCurrency: 'USD' },
+    browserRequirements: 'Requires JavaScript. Modern browser with Web Workers.',
+    featureList: [
+      'Private on-device compression — 0 bytes uploaded',
+      'HEIC, JPEG, PNG, WebP, AVIF input',
+      'Batch processing with Web Workers',
+      'Offline PWA',
+      'Never-bigger guarantee',
+    ],
+    sameAs: [
+      SITE_URL,
+      `${SITE_URL}/docs/`,
+      'https://github.com/iziuqo/compresso',
+      'https://www.npmjs.com/package/compresso.js',
+    ],
+  };
+}
+
+function seoHead(locale, meta) {
+  const canonical = appUrl(locale);
+  const hreflangLinks = Object.keys(LOCALE_SLUGS)
+    .map(
+      (loc) =>
+        `<link rel="alternate" hreflang="${HREFLANG[loc]}" href="${appUrl(loc)}" />`,
+    )
+    .join('\n    ');
+  return `
+    <title>${escapeHtml(meta.title)}</title>
+    <meta name="description" content="${escapeAttr(meta.description)}" />
+    <meta name="robots" content="index, follow" />
+    <link rel="canonical" href="${canonical}" />
+    ${hreflangLinks}
+    <link rel="alternate" hreflang="x-default" href="${appUrl('en')}" />
+    <meta property="og:title" content="${escapeAttr(meta.ogTitle)}" />
+    <meta property="og:description" content="${escapeAttr(meta.ogDescription)}" />
+    <meta property="og:url" content="${canonical}" />
+    <meta property="og:site_name" content="Compresso" />
+    <meta property="og:type" content="website" />
+    <meta property="og:image" content="${OG_IMAGE}" />
+    <meta name="twitter:card" content="summary_large_image" />
+    <meta name="twitter:title" content="${escapeAttr(meta.ogTitle)}" />
+    <meta name="twitter:description" content="${escapeAttr(meta.ogDescription)}" />
+    <meta name="twitter:image" content="${OG_IMAGE}" />
+    <script type="application/ld+json">${JSON.stringify(webAppSchema(locale, meta))}</script>`;
+}
+
+function escapeHtml(s) {
+  return s.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;');
+}
+
+function escapeAttr(s) {
+  return s.replace(/&/g, '&amp;').replace(/"/g, '&quot;').replace(/</g, '&lt;');
+}
+
+function stripSeoTags(html) {
+  return html
+    .replace(/<html lang="[^"]*">/, '<html lang="PLACEHOLDER">')
+    .replace(/<title>[^<]*<\/title>\s*/gi, '')
+    .replace(/<meta name="description"[^>]*>\s*/gi, '')
+    .replace(/<meta name="robots"[^>]*>\s*/gi, '')
+    .replace(/<link rel="canonical"[^>]*>\s*/gi, '')
+    .replace(/<meta property="og:[^"]*"[^>]*>\s*/gi, '')
+    .replace(/<meta name="twitter:[^"]*"[^>]*>\s*/gi, '')
+    .replace(/<script type="application\/ld\+json">[\s\S]*?<\/script>\s*/gi, '');
+}
+
+function patchHtml(baseHtml, locale) {
+  const meta = loadMeta(locale);
+  let html = stripSeoTags(baseHtml).replace(
+    '<html lang="PLACEHOLDER">',
+    `<html lang="${HTML_LANG[locale]}">`,
+  );
+
+  const injection = seoHead(locale, meta);
+  html = html.replace('</head>', `${injection}\n  </head>`);
+  return html;
+}
+
+function writeLocalePages(baseHtml) {
+  for (const locale of Object.keys(LOCALE_SLUGS)) {
+    const html = patchHtml(baseHtml, locale);
+    if (locale === 'en') {
+      writeFileSync(join(OUT, 'index.html'), html, 'utf8');
+    } else {
+      const dir = join(OUT, LOCALE_SLUGS[locale]);
+      mkdirSync(dir, { recursive: true });
+      writeFileSync(join(dir, 'index.html'), html, 'utf8');
+    }
+  }
+}
+
+function writeSitemap() {
+  const urls = Object.keys(LOCALE_SLUGS).map((locale) => {
+    const loc = appUrl(locale);
+    const priority = locale === 'en' ? '1.0' : '0.9';
+    return `  <url>
+    <loc>${loc}</loc>
+    <lastmod>${BUILD_DATE}</lastmod>
+    <changefreq>weekly</changefreq>
+    <priority>${priority}</priority>
+  </url>`;
+  }).join('\n');
+
+  writeFileSync(
+    join(OUT, 'sitemap.xml'),
+    `<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n${urls}\n</urlset>\n`,
+    'utf8',
+  );
+}
+
+function writeRobots() {
+  writeFileSync(
+    join(OUT, 'robots.txt'),
+    `User-agent: *\nAllow: ${APP_BASE}/\n\nSitemap: ${APP_ORIGIN}${APP_BASE}/sitemap.xml\n`,
+    'utf8',
+  );
+}
+
+const indexPath = join(OUT, 'index.html');
+if (!existsSync(indexPath)) {
+  console.error('[generate-seo] dist/compresso/index.html not found — run vite build first');
+  process.exit(1);
+}
+
+const baseHtml = readFileSync(indexPath, 'utf8');
+writeLocalePages(baseHtml);
+writeSitemap();
+writeRobots();
+console.log(`[generate-seo] Wrote 7 locale shells, sitemap.xml, robots.txt → ${OUT}`);
diff --git a/src/i18n/index.tsx b/src/i18n/index.tsx
index e3b3555..2eca323 100644
--- a/src/i18n/index.tsx
+++ b/src/i18n/index.tsx
@@ -8,6 +8,8 @@ import de from './locales/de.json';
 import it from './locales/it.json';
 import ptBR from './locales/pt-BR.json';
 import zhHans from './locales/zh-Hans.json';
+import { updateDocumentMeta } from '../seo/document-meta';
+import { localeFromPath, syncPathToLocale } from '../seo/locale-path';
 
 /**
  * Seven locales, all bundled. They are ~4 KB each: lazy-loading them would add a
@@ -45,6 +47,8 @@ export function normalize(tag: string): Locale | null {
 }
 
 export function detectLocale(): Locale {
+  const fromPath = localeFromPath();
+  if (fromPath) return fromPath;
   try {
     const stored = localStorage.getItem(STORE_KEY);
     if (stored && (LOCALE_LIST as string[]).includes(stored)) return stored as Locale;
@@ -106,6 +110,7 @@ export function I18nProvider({ children }: { children: ReactNode }) {
 
   useEffect(() => {
     document.documentElement.lang = locale;
+    updateDocumentMeta(locale);
   }, [locale]);
 
   /**
@@ -114,6 +119,7 @@ export function I18nProvider({ children }: { children: ReactNode }) {
    */
   const setLocale = useCallback((next: Locale) => {
     try { localStorage.setItem(STORE_KEY, next); } catch { /* private mode */ }
+    syncPathToLocale(next);
     setPhase('out');
     window.setTimeout(() => {
       setLocaleState(next);
diff --git a/src/i18n/locales/de.json b/src/i18n/locales/de.json
index bfa3365..276753d 100644
--- a/src/i18n/locales/de.json
+++ b/src/i18n/locales/de.json
@@ -1,4 +1,11 @@
 {
+  "meta.title": "Compresso — Bilder kostenlos online komprimieren | Privat, ohne Upload",
+  "meta.description": "Kostenloser, privater Bildkompressor. Skaliere und konvertiere HEIC, JPEG, PNG, WebP und AVIF auf deinem Gerät. Nichts wird hochgeladen. Funktioniert offline als PWA.",
+  "meta.ogTitle": "Compresso — private Bildkompression in deinem Browser",
+  "meta.ogDescription": "Komprimiere Fotos auf deinem Gerät. 0 Bytes hochgeladen. HEIC-Unterstützung, Batch-Verarbeitung, funktioniert offline.",
+  "footer.docs": "Docs",
+  "footer.faq": "FAQ",
+  "footer.npm": "npm",
   "app.tagline": "Nichts verlässt dein Gerät",
   "empty.title": "Bilder leichter machen.",
   "empty.lede": "Leg sie hier ab. Komprimiert wird auf diesem Gerät, von diesem Gerät. Nichts wird hochgeladen, und es funktioniert auch ganz ohne Verbindung.",
diff --git a/src/i18n/locales/en.json b/src/i18n/locales/en.json
index 437e453..9fd861e 100644
--- a/src/i18n/locales/en.json
+++ b/src/i18n/locales/en.json
@@ -1,4 +1,11 @@
 {
+  "meta.title": "Compresso — Compress Images Online Free | Private, No Upload",
+  "meta.description": "Free private image compressor. Resize and convert HEIC, JPEG, PNG, WebP, and AVIF on your device. Nothing is uploaded. Works offline as a PWA.",
+  "meta.ogTitle": "Compresso — private image compression in your browser",
+  "meta.ogDescription": "Compress photos on your device. 0 bytes uploaded. HEIC support, batch processing, works offline.",
+  "footer.docs": "Docs",
+  "footer.faq": "FAQ",
+  "footer.npm": "npm",
   "app.tagline": "Nothing leaves your device",
   "empty.title": "Make images lighter.",
   "empty.lede": "Drop them here. They are compressed on this device, by this device. Nothing is uploaded, and it works with no connection at all.",
diff --git a/src/i18n/locales/es.json b/src/i18n/locales/es.json
index e6c7dcf..33d746c 100644
--- a/src/i18n/locales/es.json
+++ b/src/i18n/locales/es.json
@@ -1,4 +1,11 @@
 {
+  "meta.title": "Compresso — Comprimir imágenes online gratis | Privado, sin subir",
+  "meta.description": "Compresor de imágenes privado y gratuito. Redimensiona y convierte HEIC, JPEG, PNG, WebP y AVIF en tu dispositivo. No se sube nada. Funciona sin conexión como PWA.",
+  "meta.ogTitle": "Compresso — compresión privada de imágenes en tu navegador",
+  "meta.ogDescription": "Comprime fotos en tu dispositivo. 0 bytes subidos. Soporte HEIC, procesamiento por lotes, funciona sin conexión.",
+  "footer.docs": "Docs",
+  "footer.faq": "FAQ",
+  "footer.npm": "npm",
   "app.tagline": "Nada sale de tu dispositivo",
   "empty.title": "Aligera tus imágenes.",
   "empty.lede": "Suéltalas aquí. Se comprimen en este dispositivo, con este dispositivo. No se sube nada y funciona incluso sin conexión.",
diff --git a/src/i18n/locales/fr.json b/src/i18n/locales/fr.json
index 674cbec..965d265 100644
--- a/src/i18n/locales/fr.json
+++ b/src/i18n/locales/fr.json
@@ -1,4 +1,11 @@
 {
+  "meta.title": "Compresso — Compresser des images en ligne gratuitement | Privé, sans envoi",
+  "meta.description": "Compresseur d’images privé et gratuit. Redimensionnez et convertissez HEIC, JPEG, PNG, WebP et AVIF sur votre appareil. Rien n’est envoyé. Fonctionne hors ligne en PWA.",
+  "meta.ogTitle": "Compresso — compression d’images privée dans votre navigateur",
+  "meta.ogDescription": "Compressez vos photos sur votre appareil. 0 octet envoyé. Prise en charge HEIC, traitement par lots, fonctionne hors ligne.",
+  "footer.docs": "Docs",
+  "footer.faq": "FAQ",
+  "footer.npm": "npm",
   "app.tagline": "Rien ne quitte votre appareil",
   "empty.title": "Allégez vos images.",
   "empty.lede": "Déposez-les ici. Elles sont compressées sur cet appareil, par cet appareil. Rien n’est envoyé, et tout fonctionne même sans connexion.",
diff --git a/src/i18n/locales/it.json b/src/i18n/locales/it.json
index 191d23b..27ba628 100644
--- a/src/i18n/locales/it.json
+++ b/src/i18n/locales/it.json
@@ -1,4 +1,11 @@
 {
+  "meta.title": "Compresso — Comprimere immagini online gratis | Privato, senza upload",
+  "meta.description": "Compressore di immagini privato e gratuito. Ridimensiona e converti HEIC, JPEG, PNG, WebP e AVIF sul tuo dispositivo. Non si carica nulla. Funziona offline come PWA.",
+  "meta.ogTitle": "Compresso — compressione immagini privata nel browser",
+  "meta.ogDescription": "Comprimi le foto sul tuo dispositivo. 0 byte inviati. Supporto HEIC, elaborazione batch, funziona offline.",
+  "footer.docs": "Docs",
+  "footer.faq": "FAQ",
+  "footer.npm": "npm",
   "app.tagline": "Niente esce dal tuo dispositivo",
   "empty.title": "Alleggerisci le immagini.",
   "empty.lede": "Trascinale qui. Vengono compresse su questo dispositivo, da questo dispositivo. Non si carica nulla e funziona anche senza connessione.",
diff --git a/src/i18n/locales/pt-BR.json b/src/i18n/locales/pt-BR.json
index 08ab96d..cada278 100644
--- a/src/i18n/locales/pt-BR.json
+++ b/src/i18n/locales/pt-BR.json
@@ -1,4 +1,11 @@
 {
+  "meta.title": "Compresso — Comprimir imagens online grátis | Privado, sem upload",
+  "meta.description": "Compressor de imagens privado e gratuito. Redimensione e converta HEIC, JPEG, PNG, WebP e AVIF no seu aparelho. Nada é enviado. Funciona offline como PWA.",
+  "meta.ogTitle": "Compresso — compressão privada de imagens no navegador",
+  "meta.ogDescription": "Comprima fotos no seu aparelho. 0 bytes enviados. Suporte a HEIC, processamento em lote, funciona offline.",
+  "footer.docs": "Docs",
+  "footer.faq": "FAQ",
+  "footer.npm": "npm",
   "app.tagline": "Nada sai do seu aparelho",
   "empty.title": "Deixe suas imagens mais leves.",
   "empty.lede": "Solte aqui. A compressão acontece neste aparelho, por este aparelho. Nada é enviado, e funciona até sem conexão nenhuma.",
diff --git a/src/i18n/locales/zh-Hans.json b/src/i18n/locales/zh-Hans.json
index 2ac52d5..c575a50 100644
--- a/src/i18n/locales/zh-Hans.json
+++ b/src/i18n/locales/zh-Hans.json
@@ -1,4 +1,11 @@
 {
+  "meta.title": "Compresso — 免费在线压缩图片 | 私密，不上传",
+  "meta.description": "免费私密图片压缩工具。在设备上缩放并转换 HEIC、JPEG、PNG、WebP 和 AVIF。不上传任何内容。可作为 PWA 离线使用。",
+  "meta.ogTitle": "Compresso — 浏览器中的私密图片压缩",
+  "meta.ogDescription": "在设备上压缩照片。0 字节上传。支持 HEIC、批量处理、离线可用。",
+  "footer.docs": "文档",
+  "footer.faq": "常见问题",
+  "footer.npm": "npm",
   "app.tagline": "文件不会离开你的设备",
   "empty.title": "让图片更轻。",
   "empty.lede": "把图片拖进来。压缩全部在这台设备上完成，不上传任何内容，断网也照样能用。",
diff --git a/src/seo/constants.ts b/src/seo/constants.ts
new file mode 100644
index 0000000..8ccff66
--- /dev/null
+++ b/src/seo/constants.ts
@@ -0,0 +1,50 @@
+import type { Locale } from '../i18n';
+
+/** Consumer app canonical origin + path prefix. */
+export const APP_ORIGIN = 'https://izaias.xyz';
+export const APP_BASE_PATH = '/compresso';
+export const APP_URL = `${APP_ORIGIN}${APP_BASE_PATH}`;
+
+/** Marketing / docs site (cross-links + hreflang). */
+export const SITE_URL = 'https://compresso.izaias.xyz';
+
+export const OG_IMAGE = `${SITE_URL}/og-image.jpg`;
+
+export const LOCALE_SLUGS: Record<Locale, string> = {
+  en: 'en',
+  es: 'es',
+  fr: 'fr',
+  de: 'de',
+  it: 'it',
+  'pt-BR': 'pt-br',
+  'zh-Hans': 'zh-hans',
+};
+
+export const SLUG_TO_LOCALE = Object.fromEntries(
+  Object.entries(LOCALE_SLUGS).map(([locale, slug]) => [slug, locale as Locale]),
+) as Record<string, Locale>;
+
+/** BCP-47 hreflang values. */
+export const HREFLANG: Record<Locale, string> = {
+  en: 'en',
+  es: 'es',
+  fr: 'fr',
+  de: 'de',
+  it: 'it',
+  'pt-BR': 'pt-BR',
+  'zh-Hans': 'zh-Hans',
+};
+
+export const LOCALE_LIST = Object.keys(LOCALE_SLUGS) as Locale[];
+
+export function appLocaleUrl(locale: Locale): string {
+  const slug = LOCALE_SLUGS[locale];
+  if (locale === 'en') return `${APP_URL}/`;
+  return `${APP_URL}/${slug}/`;
+}
+
+export function marketingLocaleUrl(locale: Locale, pathTail = ''): string {
+  const slug = LOCALE_SLUGS[locale];
+  const tail = pathTail ? `${pathTail.replace(/^\//, '').replace(/\/$/, '')}/` : '';
+  return tail ? `${SITE_URL}/${slug}/${tail}` : `${SITE_URL}/${slug}/`;
+}
diff --git a/src/seo/document-meta.ts b/src/seo/document-meta.ts
new file mode 100644
index 0000000..8078955
--- /dev/null
+++ b/src/seo/document-meta.ts
@@ -0,0 +1,124 @@
+import type { Locale } from '../i18n';
+import { LOCALES } from '../i18n';
+import {
+  HREFLANG,
+  LOCALE_LIST,
+  OG_IMAGE,
+  SITE_URL,
+  appLocaleUrl,
+} from './constants';
+
+type MetaDict = {
+  'meta.title': string;
+  'meta.description': string;
+  'meta.ogTitle': string;
+  'meta.ogDescription': string;
+};
+
+function metaFor(locale: Locale): MetaDict {
+  const dict = LOCALES[locale].dict as Record<string, string>;
+  const fallback = LOCALES.en.dict as Record<string, string>;
+  return {
+    'meta.title': dict['meta.title'] ?? fallback['meta.title'],
+    'meta.description': dict['meta.description'] ?? fallback['meta.description'],
+    'meta.ogTitle': dict['meta.ogTitle'] ?? fallback['meta.ogTitle'],
+    'meta.ogDescription': dict['meta.ogDescription'] ?? fallback['meta.ogDescription'],
+  };
+}
+
+function upsertMeta(name: string, content: string, attr: 'name' | 'property' = 'name') {
+  let el = document.querySelector(`meta[${attr}="${name}"]`) as HTMLMetaElement | null;
+  if (!el) {
+    el = document.createElement('meta');
+    el.setAttribute(attr, name);
+    document.head.appendChild(el);
+  }
+  el.content = content;
+}
+
+function upsertLink(rel: string, href: string, extra?: Record<string, string>) {
+  const selector = extra?.hreflang
+    ? `link[rel="${rel}"][hreflang="${extra.hreflang}"]`
+    : `link[rel="${rel}"]:not([hreflang])`;
+  let el = document.querySelector(selector) as HTMLLinkElement | null;
+  if (!el) {
+    el = document.createElement('link');
+    el.rel = rel;
+    document.head.appendChild(el);
+  }
+  el.href = href;
+  if (extra) {
+    for (const [k, v] of Object.entries(extra)) el.setAttribute(k, v);
+  }
+}
+
+function upsertJsonLd(id: string, data: object) {
+  let el = document.getElementById(id) as HTMLScriptElement | null;
+  if (!el) {
+    el = document.createElement('script');
+    el.id = id;
+    el.type = 'application/ld+json';
+    document.head.appendChild(el);
+  }
+  el.textContent = JSON.stringify(data);
+}
+
+export function buildWebApplicationSchema(locale: Locale) {
+  const meta = metaFor(locale);
+  return {
+    '@context': 'https://schema.org',
+    '@type': 'WebApplication',
+    name: 'Compresso',
+    url: appLocaleUrl(locale),
+    applicationCategory: 'MultimediaApplication',
+    operatingSystem: 'Any (web browser)',
+    description: meta['meta.description'],
+    inLanguage: HREFLANG[locale],
+    offers: { '@type': 'Offer', price: '0', priceCurrency: 'USD' },
+    browserRequirements: 'Requires JavaScript. Modern browser with Web Workers.',
+    featureList: [
+      'Private on-device compression — 0 bytes uploaded',
+      'HEIC, JPEG, PNG, WebP, AVIF input',
+      'Batch processing with Web Workers',
+      'Offline PWA',
+      'Never-bigger guarantee',
+    ],
+    sameAs: [
+      SITE_URL,
+      `${SITE_URL}/docs/`,
+      'https://github.com/iziuqo/compresso',
+      'https://www.npmjs.com/package/compresso.js',
+    ],
+  };
+}
+
+/** Sync `<head>` for the active locale (client-side navigation / language switch). */
+export function updateDocumentMeta(locale: Locale) {
+  const meta = metaFor(locale);
+  const canonical = appLocaleUrl(locale);
+
+  document.documentElement.lang = locale === 'pt-BR' ? 'pt-BR' : locale === 'zh-Hans' ? 'zh-Hans' : locale;
+  document.title = meta['meta.title'];
+
+  upsertMeta('description', meta['meta.description']);
+  upsertMeta('og:title', meta['meta.ogTitle'], 'property');
+  upsertMeta('og:description', meta['meta.ogDescription'], 'property');
+  upsertMeta('og:url', canonical, 'property');
+  upsertMeta('og:site_name', 'Compresso', 'property');
+  upsertMeta('og:type', 'website', 'property');
+  upsertMeta('og:image', OG_IMAGE, 'property');
+  upsertMeta('twitter:card', 'summary_large_image');
+  upsertMeta('twitter:title', meta['meta.ogTitle']);
+  upsertMeta('twitter:description', meta['meta.ogDescription']);
+  upsertMeta('twitter:image', OG_IMAGE);
+
+  upsertLink('canonical', canonical);
+
+  document.querySelectorAll('link[rel="alternate"][hreflang]').forEach((n) => n.remove());
+  for (const loc of LOCALE_LIST) {
+    upsertLink('alternate', appLocaleUrl(loc), { hreflang: HREFLANG[loc] });
+  }
+  upsertLink('alternate', appLocaleUrl('en'), { hreflang: 'x-default' });
+
+  upsertJsonLd('compresso-webapp-schema', buildWebApplicationSchema(locale));
+}
diff --git a/src/seo/footer-links.ts b/src/seo/footer-links.ts
new file mode 100644
index 0000000..3f1a179
--- /dev/null
+++ b/src/seo/footer-links.ts
@@ -0,0 +1,12 @@
+import { LOCALE_SLUGS, SITE_URL } from '../seo/constants';
+import type { Locale } from '../i18n';
+
+export function footerLinks(locale: Locale) {
+  const slug = LOCALE_SLUGS[locale];
+  return {
+    docs: `${SITE_URL}/docs/`,
+    faq: `${SITE_URL}/${slug}/faq/`,
+    npm: 'https://www.npmjs.com/package/compresso.js',
+    site: `${SITE_URL}/${slug}/`,
+  };
+}
diff --git a/src/seo/locale-path.ts b/src/seo/locale-path.ts
new file mode 100644
index 0000000..f855f7f
--- /dev/null
+++ b/src/seo/locale-path.ts
@@ -0,0 +1,30 @@
+import type { Locale } from '../i18n';
+import { APP_BASE_PATH, LOCALE_SLUGS, SLUG_TO_LOCALE } from './constants';
+
+const BASE = APP_BASE_PATH.replace(/\/$/, '');
+
+/** Read locale from `/compresso/` or `/compresso/{slug}/`. */
+export function localeFromPath(pathname = window.location.pathname): Locale | null {
+  const normalized = pathname.replace(/\/+$/, '') || '/';
+  if (normalized === BASE || normalized === `${BASE}/index.html`) return 'en';
+
+  const prefix = `${BASE}/`;
+  if (!normalized.startsWith(prefix)) return null;
+
+  const rest = normalized.slice(prefix.length);
+  const slug = rest.split('/')[0];
+  if (!slug) return 'en';
+  return SLUG_TO_LOCALE[slug] ?? null;
+}
+
+/** Canonical app path for a locale (always trailing slash). */
+export function pathForLocale(locale: Locale): string {
+  if (locale === 'en') return `${BASE}/`;
+  return `${BASE}/${LOCALE_SLUGS[locale]}/`;
+}
+
+export function syncPathToLocale(locale: Locale): void {
+  const next = pathForLocale(locale);
+  if (window.location.pathname.replace(/\/+$/, '') === next.replace(/\/+$/, '')) return;
+  window.history.replaceState(null, '', next);
+}
diff --git a/src/styles/app.css b/src/styles/app.css
index 44c8aee..6cbf648 100644
--- a/src/styles/app.css
+++ b/src/styles/app.css
@@ -658,6 +658,13 @@ img { max-width: 100%; display: block; }
   color: var(--ink-4);
 }
 .status__spacer { flex: 1; }
+.status__links { display: flex; align-items: center; gap: 0.55rem; }
+.status__link {
+  color: inherit;
+  text-decoration: none;
+  transition: color var(--dur-standard) var(--ease-standard);
+}
+.status__link:hover { color: var(--ink-2); }
 .status__sep { opacity: 0.5; }
 .status__sent { color: var(--ink-3); }
 .status__pulse {
diff --git a/src/ui/Chrome.tsx b/src/ui/Chrome.tsx
index b6f7cac..96d305a 100644
--- a/src/ui/Chrome.tsx
+++ b/src/ui/Chrome.tsx
@@ -1,5 +1,6 @@
 import { useEffect, useRef, useState } from 'react';
 import { LOCALES, LOCALE_LIST, T, useI18n, type Locale } from '../i18n';
+import { footerLinks } from '../seo/footer-links';
 import { GRAIN_URL } from '../design/grain';
 
 /** A whisper of film grain over everything. On a true-black ground it stops the
@@ -74,9 +75,18 @@ export function LangMenu() {
 export function StatusBar({
   done, total, running, poolSize, offline,
 }: { done: number; total: number; running: number; poolSize: number; offline: boolean }) {
-  const { t, count } = useI18n();
+  const { t, count, locale } = useI18n();
+  const links = footerLinks(locale);
   return (
     <footer className="status label">
+      <nav className="status__links" aria-label={t('footer.docs')}>
+        <a className="status__link" href={links.docs} rel="noopener">{t('footer.docs')}</a>
+        <span className="status__sep" aria-hidden="true">/</span>
+        <a className="status__link" href={links.faq} rel="noopener">{t('footer.faq')}</a>
+        <span className="status__sep" aria-hidden="true">/</span>
+        <a className="status__link" href={links.npm} rel="noopener">{t('footer.npm')}</a>
+      </nav>
+      <span className="status__sep" aria-hidden="true">/</span>
       <span><T k="status.engine" /></span>
       <span className="status__sep" aria-hidden="true">/</span>
       <span className="mono">{count(poolSize)}×</span>
diff --git a/vercel.json b/vercel.json
index f5507ba..2a6d0eb 100644
--- a/vercel.json
+++ b/vercel.json
@@ -18,6 +18,18 @@
         { "key": "Cache-Control", "value": "public, max-age=0, must-revalidate" },
         { "key": "Service-Worker-Allowed", "value": "/compresso" }
       ]
+    },
+    {
+      "source": "/compresso/sitemap.xml",
+      "headers": [{ "key": "Content-Type", "value": "application/xml; charset=utf-8" }]
+    },
+    {
+      "source": "/compresso/robots.txt",
+      "headers": [{ "key": "Content-Type", "value": "text/plain; charset=utf-8" }]
     }
+  ],
+  "rewrites": [
+    { "source": "/compresso/:locale(es|fr|de|it|pt-br|zh-hans)", "destination": "/compresso/:locale/index.html" },
+    { "source": "/compresso/:locale(es|fr|de|it|pt-br|zh-hans)/", "destination": "/compresso/:locale/index.html" }
   ]
 }
-- 
2.43.0

